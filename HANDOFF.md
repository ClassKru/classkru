# HANDOFF — ส่งต่องาน ClassKru

> เอกสารนี้เขียนไว้ให้ **Claude ตัวใหม่บนเครื่องใหม่/อีเมลใหม่** อ่านก่อนเริ่มงาน
> เพื่อให้รู้ว่าทำถึงไหน ตกลงอะไรไว้แล้ว และห้ามทำอะไรซ้ำ
> **อัปเดตล่าสุด: 26 ก.ค. 2569** · สถานะโค้ด: `main` = `1fc986d`, `index.html` = v216

---

## 0. อ่านอะไรก่อน

1. `CLAUDE.md` — กฎประหยัด token + โครงสร้างไฟล์ (**ห้ามอ่าน index.html เต็มไฟล์**)
2. ไฟล์นี้ — สถานะงาน ข้อตกลง และกับดัก
3. `Data/ปพ5-reference.md` — โครงฟอร์ม ปพ.5 ของจริง (อ่านเมื่อจะแตะ export/template)

---

## 1. สถานะ ณ วันส่งมอบ

- **ไม่มี PR ค้าง** — PR ล่าสุดคือ #40 (แก้โดเมน fallback) merge แล้ว 24 ก.ค. 2569
- working tree สะอาด ไม่มี commit ที่ยังไม่ push
- branch `fix/production-domain` = ตัวเดียวกับ main แล้ว (ลบได้)
- มี remote branch เก่าค้างเยอะ (~19 อัน) merge หมดแล้ว ยังไม่ได้ตกลงว่าจะลบ

**เฟสปัจจุบัน = Phase 1: ให้ครูใช้ฟรีทั้งหมด ยังไม่เก็บเงิน** เป้าหมายคือวัดว่ามีครูใช้จริงไหม

---

## 2. ของที่ไม่ได้อยู่ใน git — ต้องคัดลอกมาเองตอนย้ายเครื่อง

| ของ | ที่อยู่ | ทำไมสำคัญ |
|---|---|---|
| `Data/` | ในโปรเจกต์ (untracked) | `ปพ.5.xlsx` ของจริง 11 ชีต + `ปพ5-reference.md` |
| `pic/` | ในโปรเจกต์ (untracked, 3.9MB) | โลโก้ ภาพปก ไฟล์ zip จาก stitch |
| `.claude/settings.local.json` | ในโปรเจกต์ | permission ที่อนุญาตไว้ |
| memory ของ Claude 10 ไฟล์ | `~/.claude/projects/-Users-supakit-Claude-Projects-classkru-app/memory/` | เนื้อหาสำคัญถูกย่อไว้ในไฟล์นี้แล้ว |

---

## 3. บัญชีและสิทธิ์ — "ย้ายไฟล์" อย่างเดียวไม่พอ

อีเมลใหม่ต้อง **ถูกเชิญเข้า** ทุกอันนี้ก่อนถึงจะทำงานต่อได้:

- **GitHub org `ClassKru`** → repo `https://github.com/ClassKru/classkru.git`
- **Vercel team `classkru-dev`** → **production จริง = `https://classkru-kohl.vercel.app`**
- **Supabase** project ref `dzntiiuyqvkaxqpqzxeh`
- **Google Cloud** project `classkru` (OAuth client `ClassKru Web`)

⚠️ เจ้าของจริงของ Supabase/Google คือ **`classkru.dev@gmail.com`** ไม่ใช่บัญชีส่วนตัว

⚠️ `testapp-psi-seven.vercel.app` เป็น **deploy ค้างตาย** (ยังรัน `app.js?v=78` ก่อนแยกไฟล์ js) เคย hardcode อยู่ใน `auth.js` แก้ไปแล้วใน PR #40 — **อย่าเอากลับมาใช้**

⚠️ Google ไม่ให้ดู client secret ย้อนหลัง ถ้าหายต้อง **Add secret** อันใหม่ (Client ID ไม่เปลี่ยน)

---

## 4. Workflow

- **git-flow**: แก้บน `feature/*` → push → เปิด PR → **user เป็นคน merge** → Vercel auto-deploy
  (ห้ามรัน `vercel --prod`)
- ก่อน commit ที่แตะ `js/` หรือ `css/`: รัน `./bump-version.sh` แล้ว commit `index.html` ไปด้วย
- dev server: `.claude/launch.json` → `python3 -m http.server 3456`
- CI เช็ค syntax js ทุกไฟล์

**บทเรียน conflict:** ทุก PR ที่แตะ js/css จะแก้เลข `?v=` ครบ 21 จุดใน index.html → เปิด 2 PR พร้อมกันชนกันเสมอ
วิธีแก้ที่เวิร์ก: `git checkout origin/main -- index.html` แล้วแปะการแก้จริง (มักมีจุดเดียว) กลับเข้าไป แล้ว `./bump-version.sh` ให้เลขสูงกว่า main

---

## 5. สถาปัตยกรรมข้อมูล — จุดที่ต้องรู้ก่อนแตะอะไรก็ตาม

ทั้งแอปเก็บข้อมูลใน **ตารางเดียว** `classmanager_profiles (email, state jsonb, updated_at)`
ห้องเรียน/นักเรียน/คะแนน/เช็คชื่อ อยู่ใน `state` ก้อนเดียว **คีย์ด้วย email string ที่อ่านจาก localStorage** ไม่ใช่ `auth.uid()`

ผลที่ตามมา:
- ครูเปลี่ยนอีเมล = เข้าถึงข้อมูลตัวเองไม่ได้ (RLS policy ผูกกับ email ด้วย)
- ผูก LINE ในอนาคตต้องใช้ `auth.uid()`
- นับจำนวนห้องฝั่ง server ไม่ได้ เพราะเป็น blob

**RLS ปัจจุบัน** — ตรวจแล้วปลอดภัย ไม่ต้องแก้: `own_select` / `own_insert` / `own_update`
เงื่อนไข `(auth.jwt() ->> 'email') = email`
**ไม่มี DELETE policy** → คำสั่ง delete ถูกบล็อกเงียบ (คืน 0 แถว ไม่คืน error)

**Supabase config ที่ตั้งไว้แล้ว (24 ก.ค. 2569):**
- Site URL = production · Redirect URLs = `classkru-kohl.vercel.app/**` + `localhost:3456/**`
- provider Google เปิดแล้ว · Google consent screen = External + Published (scope แค่ email/profile)
- **`mailer_autoconfirm: true`** — ปิดยืนยันอีเมลอยู่ **ตั้งใจ** ในเฟสทดลอง แต่ **ต้องเปิดก่อนเก็บเงินจริง**

---

## 6. งานที่ตกลงแล้วแต่ยังไม่ได้ลงมือ (Phase 1 plumbing)

ตกลงกัน 24 ก.ค. 2569 — **ทั้งหมดนี้ทำตอนนี้ถูก ทำทีหลังแพง**

- **A2** เพิ่มคอลัมน์ `user_id UUID` เขียนค่าตอน sync (ยังอ่านด้วย email เหมือนเดิม ไม่ต้องแก้ logic) แล้วค่อยย้าย policy ไป `auth.uid() = user_id` ทีหลัง
- **A3** สร้างตาราง `subscriptions` + `payments` ไว้เฉย ๆ ให้ schema นิ่งตอนยังไม่มีข้อมูลจริง
  ฟิลด์ที่มักลืม: `grace_until` (สำคัญมากกับ manual approval), `expected_amount`, `paid_at` (คนละอันกับ created_at), `status` ต้องมี `rejected` + เหตุผล, **`transaction_id` UNIQUE** กันส่งสลิปซ้ำ
- **A4** user ใหม่ทุกคนได้ `plan = 'beta'` — ไว้แจก Premium ฟรี 1 ปีให้ครูรุ่นบุกเบิกตอนเปิดขาย
- **A5** `canUse(feature)` ใน `core.js` คืน `true` เสมอ แล้วไปเสียบตรงจุดที่จะล็อก (export เป็นหลัก) — วันเก็บเงินแก้ที่เดียว ไม่ต้องไล่ 13 ไฟล์
- **B สถิติการใช้งาน (สำคัญที่สุดในเฟสทดลอง)** — แถม 4 คอลัมน์ตอน push state ที่มีอยู่แล้ว: `last_active_at` `rooms_count` `students_count` `attend_days`
  ไม่ต้องมี event table ไม่ต้องมี analytics → จะตอบได้ว่า *"ครู 40 คนที่สมัคร เหลือ 12 คนที่ยังเช็คชื่อหลัง 1 เดือน"*
  **ต้องบอกครูตรง ๆ ในหน้าตั้งค่าว่าเก็บแค่ตัวเลขสรุป ไม่แตะชื่อเด็ก/คะแนน**
- **RLS ของ subscription:** client อ่านของตัวเองได้ เขียนไม่ได้ (service_role เท่านั้น)
  ยอมรับว่า gate ฝั่ง browser bypass ได้ — **อย่าไปสร้าง Edge Function มาปิดรูนี้** ครูกลุ่มเป้าหมายไม่ทำ ไม่คุ้มเวลา

---

## 7. เรื่องเงิน — ที่ตกลงแล้ว vs ที่ยังไม่เคาะ

**ตกลงแล้ว:**
- **email = บัญชีหลัก** (ครูใช้คอมที่โรงเรียน) · LINE เป็นของเสริม แจ้งเตือน + rich menu ลิงก์ HTML เท่านั้น **ไม่ทำ LIFF เต็ม**
- **ไม่ใช้รหัสอ้างอิงแบบ CK-58291** และ **ไม่รับสลิปทาง LINE ในเฟสแรก** → ให้อัปสลิปในเว็บตอนครูล็อกอินอยู่แล้ว (รู้ user_id 100%, ขั้นตอนน้อยกว่า, ทำบนคอมได้)
- manual approval ก่อน แล้วค่อยต่อ slip verification API ทีหลัง (SlipOK ~210฿/ด. · EasySlip ~350฿/ด.)
- **กฎเหล็ก: หมดอายุแล้วห้ามลบข้อมูล ห้ามล็อกไม่ให้ดู** — read-only + export ดิบได้เสมอ

**ยังไม่เคาะ — ห้ามตัดสินใจแทน:**
- **ราคา** ร่างไว้ เดือน 79 / เทอม 299 / ปี 499
  (Claude เสนอ: ไม่โปรโมตรายเดือน เพราะ manual approval 12 ครั้ง/ครู/ปี + ครูลืมจ่าย · และผูกวันหมดอายุกับ**ปีการศึกษา** หมด 30 เม.ย. ไม่ใช่วันที่ซื้อ)
- **free tier** — Supakit เสนอ "ฟรี 1 ห้อง" · Claude แย้งว่าครูไทยสอน 6–12 ห้อง จำกัดห้องทำให้ประเมินค่าไม่ได้แล้วเลิกใช้ก่อนจ่าย เสนอแทนเป็น **ห้องไม่จำกัด แต่ล็อก export ปพ.5** (ถ้าเอาแบบนี้ ต้องประกาศตั้งแต่วันแรก **ห้ามให้รู้ตอนสิ้นเทอม**)

---

## 8. งานค้างฝั่ง UI/โค้ด

- **รื้อ routing รอบ 2 (งานใหญ่ที่คุยไว้)** — ตัวแปรบอก "ห้องปัจจุบัน" มี 3 ตัวที่ไม่ตรงกัน (`swipeClassId` / `currentClassId` / `scoreCurrentClassId` เคยวัดเจอชี้คนละห้องพร้อมกัน) + ปุ่มกลับเป็นคนละฟังก์ชัน 4 ตัว
  แผน: เข้ารหัสห้องใน URL ทุกหน้าในห้อง (`#scores:classId` แบบเดียวกับ `#checkin:classId` ที่มีอยู่แล้ว) แล้วให้ทุกหน้าอ่านห้องจาก URL ตัวเดียว เหลือ `goBack()` ตัวเดียว
  PR #36 เดินไปครึ่งทางแล้ว (ส่ง classId เป็น param ของ `navigateToWebScreen`) แต่ยังไม่เข้ารหัสลง URL
  ⚠️ ผู้ใช้เลือกไว้ชัดว่าเอา **"เข้าทางการ์ดทางเดียว"** ไม่เอา "จำวิชาตอน refresh"
- **ชื่อห้องบนหัวจอยังไม่กลางเป๊ะ** — เบี้ยวซ้าย: คะแนน 6px · นักเรียน 32px · เช็คชื่อ 39px · การเข้าเรียน 41px (ปุ่มกลับซ้าย 61px แต่กล่องปุ่มขวาใหญ่กว่า) ยังไม่ตกลงว่าจะแลกอะไร
- **โค้ดปฏิทินซ้ำ 3 ชุด** — `dashboard.js renderCalendar` · `attendance.js renderSwipeCalendar` · `reports.js renderRepCalendar` เขียนเหมือนกันแทบทุกบรรทัด เคยทำให้หน้ารายงานตกหล่นจุดวันมีคาบสอน เสนอยุบเหลือตัวเดียวแต่ยังไม่ได้ทำ
- **โค้ดตายที่ยังไม่ลบ** — `titles` ใน `shell.js` ตั้งชื่อ 8 หน้า แต่ `#web-header-title` / `#web-header-subtitle` ไม่มีใน DOM แล้ว · DOM ของ selection-view หน้าคะแนน/รายงาน (ซ่อนถาวร เข้าไม่ถึงแล้วหลัง PR #36)
- `pic/` ยัง untracked โดยตั้งใจ — ยังไม่เคยตกลงว่าจะ commit ไหม (ไฟล์ zip/ภาพใหญ่ควรคุยก่อน)

**ยังไม่ได้พิสูจน์ — ต้องถามผลก่อนทำต่อ:**
- งานฝั่งมือถือทั้งหมดยืนยันด้วย geometry + DOM state บนเบราว์เซอร์จำลอง 375px เท่านั้น **ยังไม่เคยทดสอบบนมือถือจริง** เสี่ยงสุด: แอนิเมชันแถบแท็บ · ปัดการ์ดเช็คชื่อ · **ปุ่ม back ของ iOS Safari** (พิสูจน์ด้วย `history.back()` ใน JS ผ่าน แต่ปุ่มจริงยังไม่เคยกด) · ปุ่มค้นหาหน้านักเรียนตอนคีย์บอร์ดเด้ง
- flow **"ลบข้อมูลทั้งหมด"** หลังแพตช์ PR #39 — เช็คแค่ syntax เพราะกดจริง = ลบข้อมูลจริง ถ้าจะพิสูจน์ต้องสมัครบัญชีทดสอบแยก

---

## 9. ไอเดียที่ลองแล้ว "ถอยออก" — อย่าทำซ้ำ

1. **"จำแท็บล่าสุด"** — แตะการ์ดห้องเดิมแล้วได้หน้าไม่ซ้ำกัน ครูจำไม่ได้ → รู้สึกว่าแอปเด้งมั่ว
2. **"ธงทางเข้า" `__ckRoomEntryTab`** — เข้าใน PR #30 ถอดออกใน PR #31 วันเดียวกัน ผู้ใช้บอกว่า *"ปวดหัว"*
   → **บทเรียนรวบยอด: ห้ามให้ระบบเดาปลายทางจากสถานะที่มองไม่เห็น ไม่ว่ารูปแบบไหน** ถ้าปลายทางมีหลายอัน **ต้องมีปุ่มหลายอันให้เห็นบนจอ**
3. **ย้าย pills (มา/สาย/ขาด/ลา) ไปกลางหัวจอ** — ต้องใช้ JS ย้าย DOM ตาม breakpoint ผูกกับ event `resize` ซึ่งไม่ยิงเสมอ → pills ค้างใน `display:none` ตัวเลขหายทั้งแถบบนมือถือ ผู้ใช้สั่งยกเลิก
   ถ้าจะรื้อใหม่ต้องใช้ `matchMedia().addEventListener('change')` หรือทำ DOM สองชุดแยกจอ
4. **นำเข้าคะแนน (import Excel)** — ถอดออก 22 ก.ค. เหตุผล: จำนวนช่องคะแนนต่อคนไม่เท่ากัน (บางคนใบงาน 5 บางคน 10) โมเดล import แบบจับคู่คอลัมน์ไม่เข้ากับการใช้จริง
5. **export แบบ "ราชการ ปพ.5" (ยุบ 3 ส่วน) ให้เลือก** — ผู้ใช้บอกสับสน ถอดออก เหลือฟอร์มรายชิ้นแบบเดียว

---

## 10. โมเดลการนำทาง (2 แกน) — ยึดอันนี้เป็นหลักคิด

- **แถบแท็บ** = เปลี่ยนหน้า คาห้องเดิม
- **แตะชื่อห้องบนหัวจอ** = เปลี่ยนห้อง คาแท็บเดิม
- **การ์ดห้อง** = ประตูเข้า (แตะตัวการ์ด = เช็คชื่อเสมอ · แถวปุ่ม `.ck-class-actions` = คะแนน/นักเรียน/การเข้าเรียน)

**ข้อจำกัดที่เคยทำให้เกือบสร้างทางตัน:**
- sidebar ซ่อนบนมือถือ · bottom nav มีแค่ 5 ช่อง
- **แถบแท็บในห้องคือทางเข้าเดียวของหน้าคะแนนกับการเข้าเรียนบนมือถือ — ห้ามซ่อนอีก** (เคยเป็น `display:none !important` ทำให้คะแนนเข้าไม่ได้เลย)
- เมนู ⋮ บนการ์ดห้องมีแค่ แก้ไข/นำเข้า นร./สถิติ/ลบ — **ไม่มีคะแนนกับนักเรียน** อย่านับเป็นทางเข้า
- มือถือ: หน้าเช็คชื่อ **ไม่มีแถบแท็บ** (`#checkin-classtab-holder{display:none}`) ทางออกคือปุ่มกลับ หรือแตะชื่อห้อง

**routing:** เขียน URL ผ่าน `setRouteHash()` ใน `shell.js` เท่านั้น **อย่าสั่ง `location.hash=` ตรง ๆ**
สลับแท็บ/สลับห้อง = replace (ไม่สร้างประวัติ) · เข้าห้องจากข้างนอก = push
`switchClassTab` **ห้ามเรียก `closeSwipeAttendance()`** ตอนสลับแท็บ ให้ `ov.classList.remove('show')` ตรง ๆ แทน

---

## 11. สัญญาเรื่อง layout หัวจอ (แตะแล้วพังง่ายที่สุด)

4 หน้าในห้อง (เช็คชื่อ · คะแนน · นักเรียน · การเข้าเรียน) หัวจอต้องตรงกันเป๊ะ ไม่งั้นสลับแท็บแล้วหน้าขยับ **ผู้ใช้สังเกตเห็นทันที**

ต้นตอที่หลุดบ่อย: หน้าเช็คชื่อเป็น overlay `position: fixed` อยู่ **นอก `.app-content`** จึงไม่ได้ padding ที่ดันหน้าอื่นลงมา ต้องชดเชยด้วย margin ของตัวเองทุกครั้งที่ `.app-content` เปลี่ยน padding

| จอ | `.app-content` padding | `.swipe-class-card` margin | `.swipe-overlay` left |
|---|---|---|---|
| คอม ≥1025 | 24px | `24px 24px 0` | 250px |
| แท็บเล็ต 769–1024 | 18px | `18px 18px 0` | 208px |
| มือถือ ≤768 | 16px 12px | `16px 12px 0` | 0 |

- ระยะแนวตั้ง หัวจอ→แถบแท็บ = 16px และ แถบแท็บ→เนื้อหา = 16px (แถบแท็บมี `margin-bottom:16px` ในตัวแล้ว **อย่าใส่ margin-top ซ้ำ** จะกลายเป็น 32)
- ปุ่มมุมขวาบนใช้ `.ck-hdr-action` สูง 39px (คอม) / 30px (มือถือ) · ปุ่มที่มีสีของตัวเองใส่ `.is-tinted`
- คลาสกลางต้องอยู่ **หลัง** `.btn`/`.form-control` ในไฟล์ ไม่งั้นแพ้ specificity
- **กับดัก:** `.rep-detail-header-card > div > div:first-child > div { flex:1 }` โดน **ทุก div ลูก** รวมกล่องปุ่ม → หน้านักเรียนเคยถูกยืดจาก 66 เป็น 124px แก้ด้วย `.students-hdr-actions { flex: 0 0 auto !important }`
- คลาสใช้ร่วมหลายหน้า (`.d-student-header-row`, `.rep-detail-header-card`, `.cal-day`) ถ้าจะแก้เฉพาะหน้าให้ scope ด้วย id เช่น `#swipe-desktop-view .d-student-header-row`

---

## 12. คอนเวนชัน UI ที่ตั้งไว้แล้ว — ใช้ซ้ำ อย่าประดิษฐ์ใหม่

- **avatar วงกลม = เลขที่เสมอทั้งแอป** (ยกเว้นการ์ดรายชื่อคะแนนวงใหญ่ = ชื่อเล่น) · พาเลตต์ `MSC_AV` ใน `scores.js` (6 คู่พาสเทล ไล่ตาม index คนในห้อง → คนเดียวกันสีตรงทุกหน้า) ส่งสีผ่าน CSS var `--av-bg/--av-fg`
- **สีเกรด:** `mscGradeClass()` (high/mid/low/zero) · `#3b6d11` `#854f0b` `#993556` `#a32d2d` · คลาส `.g-c-*` (desktop) / `.msc-g-*` (มือถือ)
- **ไอคอนหัวจอ:** `.ck-ic-purple/blue/gray i` — เครื่องมือ=ม่วง ค้นหา=ฟ้า พิมพ์=แดง Excel=เขียว
- **สีประจำวันไทย:** `THAI_DAY_COLORS` ใน `attendance.js`
- **ปุ่มทั้งแอปโค้ง 18px** ผ่าน token เดียว `--radius-btn`
- หน้าคะแนนมือถือแยกด้วย `isMobileView()` (max-width 768px) ใน `renderScoreMatrix` → `renderMobileScores` · ตัวจำคนที่เปิด = `mobileScoreStudentId`
- **ส่งออกคะแนน:** `exportScoresExcel` / `exportScoresPDF` ท้าย `scores.js` · โครงไฟล์ = `buildScoreSheet(c, true)` · **PDF = พิมพ์ผ่านเบราว์เซอร์** (`window.open`+`print`, @page A4 landscape) ไม่ใช้ jsPDF

---

## 13. กับดักตอนทดสอบ (เคยทำให้สรุปผิดมาแล้ว)

**แคช:** `./bump-version.sh` อัป `?v=` ให้ js/css ทั้ง 21 จุด **แต่ไม่ช่วยเรื่อง index.html เอง** — reload ธรรมดาได้ HTML เก่าที่ชี้ `?v=` เก่า
→ ต่อ query ไม่ซ้ำท้าย URL ทุกครั้ง เช่น `http://localhost:3456/index.html?cb=217`
→ เช็คว่าได้ของใหม่จริง: `document.querySelector('link[href*="01-base"]').getAttribute('href')`
→ เปลี่ยนแค่ hash (`#scores` → `#checkin`) **ไม่ทำให้หน้าโหลดใหม่** ยิงแค่ `hashchange` (เคยเข้าใจผิดว่า deep-link พัง)

**Browser pane:**
1. **แท็บที่ไม่ถูก render คืนค่า 0 ทั้งหมด** — เคยวัดหัวจอได้ 94px ทั้งที่จริง 52px แล้วเกือบไล่แก้ผิดจุด
   เช็คก่อนวัดทุกครั้ง: `document.body.getBoundingClientRect().width` ต้อง > 0 · ปิดแท็บส่วนเกิน
2. **`requestAnimationFrame` และ event `resize` ไม่ยิง** ใน pane นี้ — และห้ามเขียนโค้ดที่พึ่งสองตัวนี้ทางเดียว เพราะบนมือถือจริงตอนแท็บถูกซ่อนก็ไม่ยิง ใช้ forced reflow (`void el.offsetWidth`) หรือ `setTimeout` สำรอง
3. **transition ไม่เดินเฟรม** — `getComputedStyle()` ค้างที่ค่าเริ่มต้น ดูเหมือนแอนิเมชันพัง
   ยืนยันด้วย `el.getAnimations()` · ก่อนวัดขนาดสั่งให้แอนิเมชันจบก่อน:
   `document.querySelectorAll('*').forEach(e=>e.getAnimations().forEach(a=>a.finish()))`

---

## 14. วิธีทำงานกับ Supakit (สำคัญไม่แพ้เรื่องเทคนิค)

**วัดจริงก่อนสรุปเสมอ** — เขาตรวจงานด้วยตาละเอียดมากและจับได้เมื่อคำตอบมาจากการเดา
เคยตอบว่า "เท่ากันแล้ว" 6 ครั้งติดโดยวัดแต่แนวนอน จนโดนสวนว่า *"ยังไงก็ไม่เท่า ช่วยวัดขนาดจากเต็มหน้า หรือวัดจากขอบบนสิ"* แล้วปรากฏว่าต่างกันจริง
→ วัดทุกมิติ (บน/ซ้าย/กว้าง/สูง) ทุก breakpoint ก่อนบอกว่าเสร็จ · แสดงตารางเทียบก่อน–หลัง · **ถ้ายืนยันด้วยตาไม่ได้ ให้บอกตรง ๆ ว่าพิสูจน์ได้แค่ไหน**

**"ถามก่อน ค่อยทำ" หมายความตามนั้น** — เขาพิมพ์กำกับบ่อย ให้เสนอทางเลือกพร้อมข้อดีข้อเสียและตัวเลขประกอบ แล้วรอ · ชอบให้ทำ mockup เทียบกันก่อนตัดสินใจ

**แย้งได้และควรแย้ง** — ถ้าเห็นว่าทางที่เขาเสนอมีปัญหา ให้บอกตรง ๆ พร้อมเหตุผลเชิงกลไก **อย่าทำตามเงียบ ๆ** (เคยแย้งแล้วเขาตอบว่า *"ฉันเข้าใจคุณแล้ว"* แล้วเปลี่ยนแนวทางทันที)

**เปลี่ยนใจได้ ให้ย้อนกลับให้สะอาด** — สั่ง *"ยกเลิก เอากลับเป็นแบบเดิม"* กลางทางได้ ให้ลบให้หมดแล้ว `grep` ยืนยันว่าไม่เหลือเศษ พร้อมอธิบายว่าทำไมของเดิมพัง

**ภาษา:** สื่อสารภาษาไทย · พิมพ์เร็วสะกดผิดบ่อย ให้ตีความจากบริบทและรูปที่แนบมา · ส่งสกรีนช็อตมาชี้ปัญหาเป็นหลัก

---

## 15. เช็กลิสต์ตอนตั้งเครื่องใหม่

- [ ] เชิญอีเมลใหม่เข้า GitHub org `ClassKru` / Vercel team `classkru-dev` / Supabase / Google Cloud
- [ ] `git clone https://github.com/ClassKru/classkru.git`
- [ ] คัดลอก `Data/` และ `pic/` มาวางในโปรเจกต์ (ถ้ายังไม่ได้ commit)
- [ ] คัดลอก `.claude/settings.local.json`
- [ ] ทดสอบ dev server: `python3 -m http.server 3456` แล้วเปิด `http://localhost:3456/index.html?cb=1`
- [ ] ตรวจว่า login Google/email บน production ยังทำงาน
- [ ] ให้ Claude ตัวใหม่อ่าน `CLAUDE.md` + ไฟล์นี้
